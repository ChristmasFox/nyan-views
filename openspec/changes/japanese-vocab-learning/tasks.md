## 1. 首页与演示代码迁移

- [x] 1.1 将原 `HomePage` 演示 UI 迁至 `LegacyDemoHomePage`（含 `DisplayGroup` 类型），默认路由不再引用
- [x] 1.2 `app/page.tsx` 移除 `displayGroups` 查询，仅渲染日语学习首页
- [x] 1.3 实现日语学习首页：JLPT 定位、主入口、模式说明、每日路线/study 占位，响应式布局

## 2. 学习会话与路由骨架

- [x] 2.1 新增 `/study`（或设计选定路径）占位页，承接首页 CTA
- [x] 2.2 会话前模式选择 UI（打字 / 语音）与 localStorage 记忆上次选择

## 3. 词表与 SRS 核心

- [x] 3.1 词条数据模型与近似 N5–N2 标签；导入首批 N5 JSON/TS 常量
- [x] 3.2 集成 SRS 调度（库或 SM-2）；IndexedDB 或等价持久化卡状态
- [x] 3.3 级别单选与混选队列构建

## 4. 卡片 UI 与作答

- [x] 4.1 叠卡/翻面组件与当前卡状态机
- [x] 4.2 打字模式：仅输入与答案规范化、判定
- [x] 4.3 语音模式：识别 → 重试 → 打字兜底；宽松相似度与可配置阈值

## 5. 发音与反馈

- [x] 5.1 TTS 播放抽象与日语 voice 选择
- [x] 5.2 成功音效资源与开关；尊重 `prefers-reduced-motion`（与动效策略一致）

## 6. 每日路线与 streak

- [x] 6.1 每日路线进度模型与 UI
- [x] 6.2 本地 streak 与时区切日逻辑
- [x] 6.3 首页或专页展示连续打卡与今日完成态

## 7. 收尾与质量

- [x] 7.1 移动端实测语音权限与键盘遮挡
- [x] 7.2 更新 `layout` metadata 与 `lang`（如 `ja` 或 `zh-CN` 按产品定）
- [ ] 7.3 归档本变更（`openspec archive`）前对照 spec 自测
